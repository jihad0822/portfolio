/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // Optional: Optimize for static hosting
  trailingSlash: true,
  // Add other Next.js configurations as needed
};

module.exports = nextConfig;